# 법령 확장 인수인계

작성: 2026-09-22. 리뷰 기준: `63720d3` (`main`, GitHub 푸시 완료).

## 먼저 읽을 요약

**기존 구조를 유지하면서 저장·조문 인식 문제를 고친 뒤, 형식이 다른 법령 3~5개를 추가해 검증한다.** 전체 법령 수집은 이후 단계다.

사용자와 합의한 순서:

1. 같은 DB에 법령별로 처리·저장하되 긴 법령도 청크 단위로 나눠 넣는다.
2. 새 인덱스가 완성된 뒤 교체하여 실패 시 기존 데이터를 보존한다.
3. 법률명·조문·본문/부칙 구분과 인용 검증을 보완한다.
4. 소수 법령을 추가하고 발견된 문제를 고친 뒤 점진적으로 확대한다.

**1단계 저장과 2~7번 검색·인식 수정은 완료했다.** 기존 두 PDF와 합성 입력으로 확인했다. 다음 단계는 형식이 다른 법령 3~5개를 추가해 새 데이터에서 다시 검증하는 것이다. 모든 구조적 문제가 해결된다고 보장한 상태는 아니다.

사용자 추가 결정: **글자수 청킹은 과제 비교용으로 짧은 법률 한 개만 임베딩한다.** 현재 `common.LENGTH_DOCUMENT`는 두 기존 PDF 중 짧은 근로기준법이다. 앞으로 새 법령을 추가해도 `article`에만 모두 넣고, `length`에는 이 한 개만 유지한다.

사용자는 사용량 절약과 쉬운 설명을 원한다. 전체 리뷰를 반복하지 말고 아래 재현과 완료 조건을 활용한다. 별도 요청 없이 하위 에이전트를 생성하지 않는다.

## 환경과 범위

- `AGENTS.md`, `README.md`를 먼저 읽는다. 실행 코드: `common.py`, `build_index.py`, `rag.py`, `app.py`. `back_up/`은 실행하지 않는 보관본이다.
- Python: `C:\Users\redye\anaconda3\envs\rag_challenge\python.exe` (3.12).
- 설치된 Chroma: `1.5.9`. 주요 패키지 버전은 `requirements.txt`와 일치함을 확인했다.
- 문서: `data/laws/law.pdf`(저작권법), `data/laws/labor_standards_act.pdf`(근로기준법). 등록은 `common.DOCUMENTS`에서 한다.
- `data/index/`, `data/cache/`는 이미 있는 산출물이며 Git 추적 대상이 아니다. 기존 인덱스는 실패 실험에 사용하지 않는다.
- GPU 자동 감지와 Windows DLL/PATH 보정은 실제 환경 문제 대응 코드이므로 유지한다.
- API 키를 출력·커밋하지 않는다. 아래 로컬 검증에는 OpenAI 호출이 필요 없다.
- 이번 문서 저장에는 커밋·푸시 요청이 포함되지 않는다. 이전 커밋의 푸시 승인을 향후 모든 푸시 승인으로 확대하지 않는다.

## 확인된 문제와 수정 완료 조건

### 1. 저장 요청 한도와 실패 시 인덱스 손실 — 수정 완료

위치: `build_index.py::save_to_chroma`, `build_strategy`, `embed_cached`.

수정 전에는 기존 컬렉션 삭제 → 재생성 → 전체 청크를 한 번에 `add()`했다. 임시 DB에서 `client.get_max_batch_size()`는 **5,461**이었다. 기존 레코드가 있는 컬렉션에 예전 함수로 **5,462개**를 넣자 아래 오류가 발생하고 컬렉션이 **0개**로 남았다.

```text
InternalError ValueError: Batch size of 5462 is greater than max batch size of 5461
```

이것은 **한 요청의 청크 수 제한**이며 DB 전체 저장 용량 제한이 아니다.

구현 내용:

- 법령별로 같은 전략 컬렉션에 추가하고, 법 하나도 `min(1000, client.get_max_batch_size())`씩 나눠 저장한다. 배치의 ID·문서·벡터·메타데이터 범위와 법령 사이 ID가 겹치지 않게 했다.
- `data/index/builds/<빌드 ID>/`에 새 Chroma DB를 만들고 두 전략이 완성되면 `active.txt`를 원자적으로 교체한다. `active.txt`가 없으면 종전 DB 경로를 사용한다. 실행 중인 앱은 재시작 후 새 DB를 읽는다. 이전 빌드는 자동 삭제하지 않는다.
- 임베딩 캐시는 법령별로 계산한다. 기존 전체 묶음 캐시와 이름이 달라 첫 빌드에서 다시 임베딩한다.
- **법령별 저장과 증분 갱신은 별도 기능**이다. 우선 안전한 전체 빌드를 완성한다. 문서별 추가·개정만 반영하는 증분 갱신은 이후 작업으로 남겨도 된다.
- 수정 전에는 임베딩 캐시가 전체 텍스트 묶음 기준이었다. 지금은 법령별 캐시를 사용한다.

검증: 임시 DB에 한도보다 많은 5,462개 청크와 기존 1개를 저장해 ID 중복 없이 총 5,463개를 확인했다. 두 법령·두 전략의 새 빌드 후 다음 빌드의 두 번째 전략을 강제로 실패시키자 기존 활성 경로와 두 컬렉션의 6개씩이 유지됐다. 사용자 요청에 맞춰 `length`를 근로기준법 하나로 줄인 뒤 실제 PDF와 GPU 임베딩 모델로 다시 빌드했다. 현재 활성 DB는 `rules_article` 819개(저작권법 519, 근로기준법 300), `rules_length` 117개(근로기준법만)다.

### 2. 질문에 적힌 법률명을 무시하는 조문 번호 검색 — 수정 완료

위치: `rag.py::lookup`, `search`.

실제 두 PDF에서 만든 임시 DB에 `근로기준법 제1조가 뭐야?`를 조회하니 저작권법 제1조도 반환됐다. `lookup()`은 번호만 추출하며 직접 조회 결과는 `top_k`보다 많아도 모두 답변 입력에 포함된다.

법률명과 조문 번호를 연결해 조회하고 UI 문서 필터와 교집합으로 적용한다. 긴 이름(`저작권법 시행령`)을 짧은 이름(`저작권법`)으로 착각하지 않아야 한다. `저작권법 제1조와 근로기준법 제2조 비교`도 처리해야 하므로 첫 법 이름을 질문 전체에 무조건 적용하지 않는다. 법률명이 없는 모호한 번호 질문의 동작도 명시하고 모든 법령의 해당 조문을 무제한 입력하지 않는다.

완료 조건: 단일 법률 조회, 여러 법률 비교, UI 필터 충돌, 법률명 없는 질문을 확인한다. 재작성된 질문에도 적용하고 동일 조문의 여러 청크를 보존한다.

### 3. 인용 파싱 누락이 검증 통과처럼 보임 — 수정 완료

위치: `rag.py::_DOC_NAMES`, `CITE_TOKEN`, `parse_citations`, `verify_citations`, `cited_articles`.

| 수정 전에 확인한 입력 | 수정 전 결과 |
| --- | --- |
| `근거: 민법 제999조` (민법 미등록) | 파싱과 검증 경고 모두 `[]` |
| `근거: **근로기준법 제1조 제99항**` | 파싱과 검증 경고 모두 `[]` |
| `근거: 근로기준법 제1조 제99항` | 정상적으로 잘못된 항을 경고 |
| `근거: 저작권법 시행령 제1조` (시험 중 등록) | 파싱 `[]` |
| `근거: 정보통신망 이용촉진 및 정보보호 등에 관한 법률 제1조` (시험 중 등록) | 파싱 `[]` |

정규식의 모든 요소가 선택적이라 빈 문자열이나 짧은 법률명만 먼저 매칭할 수 있다. 법률명을 읽기 전에 `및`으로 나누는 것도 이름을 깨뜨린다. 파싱에서 사라진 인용은 검증 루프에 들어가지 않는다.

법률명 전체를 먼저 인식하고 강조 표시도 처리한다. 인용 줄에 해석하지 못한 내용이나 모르는 법률명이 있으면 검증 경고로 남긴다. 기존 생략 표기(`근로기준법 제48조 제1항, 제2항`)는 유지한다. 반환 형식을 바꾸면 기억 저장·화면 경고까지 반영한다. `문서에서 확인되지 않습니다.`처럼 정상 거절하는 답변을 무조건 오류 처리하지 않는다.

완료 조건: 위 표와 정상 인용·생략 인용을 검증하고, 실패한 인용이 기억에 정상 근거처럼 남는지도 확인한다.

### 4. 괄호 제목 없는 조문 누락 — 수정 완료

위치: `build_index.py::ARTICLE`, `chunk_by_article`, `chunk_by_length`.

```python
chunk_by_article("제1조 첫째 본문\n제2조 둘째 본문")
# 수정 전 결과: []
```

수정 전에는 `제N조(제목)` 또는 `제N조 삭제`만 인식했다. 현재는 제목 없는 형식도 청킹하며, 문서가 0청크이면 빌드를 중단한다.

주의: 단순히 줄 시작 `제N조`를 모두 조문으로 취급하면 본문 참조도 잘린다. 기존 PDF에도 줄 시작 `제26조부터 제28조까지, ...`, `제37조를 준용한다...`가 있다.

완료 조건: 제목 유무, 삭제 조문, 가지번호(`제35조의2`), 본문 속 조문 참조의 경계가 맞고 문서 전체 누락이 조용히 지나가지 않아야 한다.

### 5. 번호 없는 부칙이 마지막 본문에 붙음 — 수정 완료

위치: `build_index.py::chunk_by_article`, `scan_heading`.

```python
chunk_by_article("제1조(목적) 본문\n부칙 <법률 제123호>\n이 법은 공포한 날부터 시행한다.")
```

수정 전에는 시행일 문장이 제1조 본문에 붙었다. 현재는 부칙 경계에서 본문을 끝내고 번호 없는 부칙 내용을 별도 청크로 저장한다.

완료 조건: 번호 있는/없는 부칙 모두 본문에서 분리되고 부칙 제외 검색에 섞이지 않아야 한다.

### 6. 본문과 부칙의 같은 조문 번호가 검증에서 합쳐짐 — 수정 완료

위치: `rag.py::verify_citations`의 상세 조회와 식별 키, 청킹 메타데이터.

합성 문서의 본문 제1조에 ①②, 부칙 제1조에 ①②③을 넣었다. 본문만 검색해도 `근거: 저작권법 제1조 제3항`이 통과했다. 상세 조회가 `(doc, article_no)`만 사용해 부칙의 ③까지 읽기 때문이다.

본문/부칙 구분을 식별에 반영한다. 여러 개정 부칙은 `chapter="부칙"`만으로 서로 구별되지 않으므로 필요한 식별 정보도 남긴다. 검증을 검색된 일부 청크로만 제한하면 과거 오탐 문제가 재발하므로 **동일 조문의 전체 청크를 조회하는 방식**은 유지한다.

완료 조건: 본문 검증에 부칙 항·호가 섞이지 않고, 같은 조문의 검색되지 않은 청크에 있는 실제 항은 유효하게 검증해야 한다.

### 7. 글자수 전략의 메타데이터가 끝 조문 기준임 — 수정 완료

위치: `build_index.py::chunk_by_length`의 `flush()`.

```python
chunk_by_length("제1조(목적) 첫째 본문\n제2조(정의) 둘째 본문", chunk_size=400, overlap=0)
# 한 청크가 생성되고 article_no는 제2조. 함수 설명은 시작 위치 기준이라고 되어 있음.
```

수정 전에는 저장 시점의 `cur_article`을 써서 제1조 내용이 있어도 제1조 번호 조회에서 빠졌다. 현재는 시작 조문과 포함된 모든 조문 번호를 기록한다. 여러 조문을 포함하는 청크의 인용 항·호는 조문 단위 컬렉션에서 확인한다. 비교용 글자수 분할은 유지한다.

## 진행 체크리스트

- [x] 저장 배치 분할, ID 중복 방지, 실패 시 기존 인덱스 보존(임시 DB 검증).
- [x] 조문 경계와 본문/부칙 식별 보완. 합성 입력과 기존 PDF 청킹 결과 확인.
- [x] 법률별 조회와 인용 파싱 보완. 정상·잘못된 인용, 이름 접두어와 `및` 사례 확인.
- [x] 기존 두 PDF로 재빌드. 조문 전략 819개(두 법률), 글자수 전략 117개(근로기준법만). 두 전략의 법률별 직접 조회와 항 검증 확인. 활성 빌드 ID: `8c982765e1d24b349072d03d924fc071`.
- [ ] 형식이 다른 법령 3~5개 추가: 긴 법령, 제목 없는 조문, 긴 이름/`및` 포함 이름, 다양한 부칙 형식 등을 포함해 선정. 시행령을 추가한다면 법률명 접두어 충돌도 확인.
- [ ] 대표 질문으로 답변과 표시 근거를 확인하고 남은 한계를 README에 기록.

법령 추가 시 출처·수집일·시행 기준을 기록하고 README의 시행예정조문 제외 지침을 따른다. PDF 유지/API 활용 중 수집 방식은 아직 결정하지 않았다. 전체 자동 수집, 분야별 UI, 전면적인 구조 변경은 이번 선행 수정에 묶지 않는다.

## 검증 방법과 한계

- 리뷰는 실제 두 PDF와 합성 입력을 사용했다. DB 실험은 별도 임시 Chroma DB에서 고정 벡터로 수행했으므로 **의미 검색 품질을 측정한 결과는 아니다.**
- `RagBot.__new__(RagBot)`으로 만든 객체에 임시 `collection`을 연결하면 모델/API 없이 `lookup()`과 `verify_citations()`를 확인할 수 있다.
- 저장 함수 실험은 모듈의 DB 경로를 임시 경로로 바꿔 실행한다. 실제 `data/index/`를 삭제하거나 비우지 않는다.
- LLM 생성, 실제 임베딩 추론, Streamlit 전체 흐름은 리뷰에서 실행하지 않았다. 구현 후 변경에 필요한 범위에서 확인한다.
- 테스트는 위 재현과 중요한 회귀 사례에 집중한다. 매번 전체 임베딩/API 호출을 반복하지 않는다. 실제 청킹 변경 후 최종 통합 확인 때 재빌드한다.
- PowerShell에서 한글 코드를 Python 표준입력으로 보낼 때 아래 설정을 사용한다. 없으면 한글이 `?`로 바뀌어 검증이 깨질 수 있다.

```powershell
$OutputEncoding = [System.Text.UTF8Encoding]::new($false)
$env:PYTHONIOENCODING = 'utf-8'
@'
# 검증용 Python 코드
'@ | & 'C:\Users\redye\anaconda3\envs\rag_challenge\python.exe' -
```

문서 불일치: AGENTS.md에는 항 기호 없는 조문을 관대하게 통과시킨다고 되어 있지만 현재 코드와 README는 존재하지 않는 항을 경고하는 상태다. 이 차이는 동작 버그로 지적하지 않았다. 6번 재현은 본문에 ①②가 있으므로 이 불일치와 별개다. 이번 수정 중 항 기호 없는 조문의 정책을 임의로 바꾸지 않는다.

사용자에게는 이번 단계에서 고친 내용과 실행으로 확인한 결과를 짧고 쉽게 설명한다.
