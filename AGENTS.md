# AGENTS.md

프로젝트 개요·배경·아키텍처는 [README.md](README.md) 참고. 이 파일은 코드를 다루는 에이전트(리뷰/수정)를 위한 실행 지침만 담는다.

## 실행

```bash
conda activate rag_challenge   # 없으면 README의 빠른 시작 참고
python build_index.py          # 문서/청킹 규칙 바뀔 때만
streamlit run app.py           # 또는: python rag.py (터미널)
```

`.env`에 `OPENAI_API_KEY` 필요 (`.env.example` 참고). `data/index/`, `data/cache/`는 `build_index.py`가 만드는 산출물이라 git에 없음 — 코드만 보고 "빌드가 안 됐다"고 오해하지 말 것. 원본 PDF는 `data/laws/`에 둔다.

## 코드 스타일

- 주석은 "왜"가 비직관적일 때만, 한국어로 짧게. "무엇을 하는지"는 설명하지 않음 (변수/함수명으로 이미 드러남).
- 과한 추상화·방어적 코드 지양. 이 프로젝트 규모(법령 PDF 2개, 청킹 전략 2개)에 맞는 만큼만 일반화되어 있음 — 문서/전략이 실제로 늘어나기 전까지 더 유연하게 만들 필요 없음.
- `common.py`의 GPU 자동 감지, PATH 보정 코드처럼 겉보기에 과해 보이는 부분은 실제로 겪은 환경 문제(cuDNN DLL 미탐지 등)의 수정이지 방어적 코드가 아님 — README "GPU/CPU 자동 전환" 절 참고 후 판단할 것.

## 리뷰 시 참고

- **근거 검증 로직**(`rag.py`의 `verify_citations`)은 항(①②) 기호가 없는 조문을 "조문 전체"로 간주해 관대하게 통과시킴 — 의도된 동작이지 버그 아님 (README "알려진 한계" 참고).
- 리팩토링 제안 시, 실제 죽은 코드/불일치/버그가 아니라면(스타일 취향 수준이면) 굳이 고치지 않는 걸 선호함.
- 클레임(예: "이 코드가 동작한다", "이 경로가 실행된다")은 가능하면 실제 실행/테스트로 확인한 뒤 말할 것.
